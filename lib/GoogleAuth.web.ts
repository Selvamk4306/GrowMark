// Google Sign-In is a native-only module and cannot run in a browser.
// This web stub is served automatically by Metro/Expo on the web platform,
// preventing the native module from being bundled and crashing on load.

export const signInWithGoogle = async (): Promise<null> => {
  throw new Error('Google Sign-In is not supported on web.');
};
